<?xml version="1.0" encoding="UTF-8"?>
<!--
  macOS LaunchAgent for the queqiao local client.

  deploy/install-client.sh generates this agent with the real paths already
  filled in. This template is the hand-installed path.

  Install:
    cp deploy/me.01.queqiao.client.plist ~/Library/LaunchAgents/
    # edit every /Users/YOU path and the profile name below
    launchctl bootstrap "gui/$(id -u)" \
      ~/Library/LaunchAgents/me.01.queqiao.client.plist

  Restart after upgrading the binary or changing configuration:
    launchctl bootout "gui/$(id -u)/me.01.queqiao.client"
    launchctl bootstrap "gui/$(id -u)" \
      ~/Library/LaunchAgents/me.01.queqiao.client.plist

  kickstart restarts the definition launchd already cached and does not re-read
  edited arguments from disk, so it is not enough after changing this file.

  See docs/DEPLOYING.md for the egress side and the Clash profile.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>me.01.queqiao.client</string>

  <key>ProgramArguments</key>
  <array>
    <string>/Users/YOU/.queqiao/bin/queqiaod</string>
    <string>client</string>

    <!-- The single profile imported from the provider invitation contains the
         endpoint, pinned provider identity, and this device's private key. -->
    <string>--profile</string>
    <string>/Users/YOU/.config/queqiao/PROVIDER-ID.json</string>

    <!-- The SOCKS5 ingress Clash forwards to. Loopback only. -->
    <string>--listen</string>
    <string>127.0.0.1:12080</string>

    <!-- Not optional when Clash runs in TUN mode. Clash installs a default
         route through 198.18.0.1, so without this binding queqiao's own outer
         connection is captured by Clash and tunnelled through whatever proxy
         Clash is using. if:en0 re-resolves the physical address before each
         dial, so DHCP changes are handled. Verify with tcpdump on the egress
         host that the source address arriving is your real uplink. -->
    <string>--local-address</string>
    <string>if:en0</string>

    <!-- Loopback-only counters: flows, lanes, fallbacks, controller state. -->
    <string>--metrics-listen</string>
    <string>127.0.0.1:12090</string>

    <string>--log-level</string>
    <string>info</string>

    <!-- A machine-readable, bounded runtime log. queqiaod creates the parent
         directory and rotates at 32 MiB with five backups by default. -->
    <string>--log-file</string>
    <string>/Users/YOU/Library/Logs/Queqiao/client.log</string>
    <string>--log-format</string>
    <string>json</string>
    <string>--log-stderr=false</string>
    <string>--telemetry-log-interval</string>
    <string>5s</string>
  </array>

  <key>RunAtLoad</key>
  <true/>

  <!-- Restarts on an unsuccessful process exit. The cancelled-transfer stall
       that previously left a live but unusable process is fixed and covered
       by deterministic regressions; use kickstart after upgrades or edits. -->
  <key>KeepAlive</key>
  <dict>
    <key>SuccessfulExit</key>
    <false/>
  </dict>

  <key>ProcessType</key>
  <string>Interactive</string>
</dict>
</plist>
