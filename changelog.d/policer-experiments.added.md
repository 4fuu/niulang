Add an uncompensated fixed-wire-rate QUIC control and reproducible policer,
connection-reuse, loss-resilience, and application-UDP benchmark suites for
evaluating latency, delivery, completion, and sender-induced drops together;
add an opt-in shared per-path QUIC pacing cap which preserves erasure/FEC and
reserves capacity from bulk connections without changing the default.
